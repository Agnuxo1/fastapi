# vercel-fastapi-starter 0.1.0 — preserved source

This directory preserves the complete 14-file tracked tree from commit
`2d4507c3a26da127fdf4bc143c5bc0073889d8b3`.

- `source-2d4507c.tar.gz` is the baseline Git archive.
- `MANIFEST.json` records every tracked file and its SHA-256 digest.

The archive is retained for audit and rollback reference, not as a recommendation
to deploy the historical starter unchanged.
